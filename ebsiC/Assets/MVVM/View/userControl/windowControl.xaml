<UserControl x:Class="ebsiC.Assets.MVVM.View.userControl.windowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ebsiC.Assets.MVVM.View.userControl"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="48" d:DesignWidth="146">
    <Menu IsMainMenu="True">
        <MenuItem Header="{materialDesign:PackIcon Kind=WindowMinimize}" 
                  Click="window_Minimize"
                  Style="{StaticResource MaterialDesignMenuItem}"/>
        <MenuItem Header="{materialDesign:PackIcon Kind=WindowMaximize}" 
                  Click="window_Maximize"
                  Style="{StaticResource MaterialDesignMenuItem}"/>
        <MenuItem Header="{materialDesign:PackIcon Kind=WindowClose}" 
                  Click="window_Close"
                  Style="{StaticResource MaterialDesignMenuItem}"/>
    </Menu>
</UserControl>
